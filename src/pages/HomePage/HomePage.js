import React from 'react';
import styles from './HomePage.module.css';

const Home = () => (
  <div className={styles.titleWrapper}>
    <h2 className={styles.title}> Welcome to Paws online store! </h2>
  </div>
);

export default Home;
